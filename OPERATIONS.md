# 運用ルール（令和8年熊本地震 支援情報まとめ）

このサイトの運用は「更新日時の管理」と「リンクの健全性維持」の2点に絞り、どちらも自動化しています。人が判断するのは掲載内容だけです。

## 1. 更新日時の書き換え —— 自動

- `index.html` を編集して `main` に push すると、GitHub Actions（`update-timestamp.yml`）がページ内の「最終更新」を **その日の日付（日本時間）** に自動で書き換えてコミットします。
- **手で日付を書き換える必要はありません。** 情報を直したら push するだけです。
- 逆に、内容を変えていないのに日付だけ新しくしないこと（読者に「確認済み」と誤解させるため）。

## 2. リンク確認 —— 毎朝自動 + 発見時の対応手順

- 毎朝6時（JST）に GitHub Actions（`link-check.yml`）が全リンクを自動チェックします。
- リンク切れがあると **Issue が自動で立ちます**（ラベル: `リンク切れ`）。
- Issue が立ったときの対応（当日〜翌日中）:
  1. リンク先が移転 → 新URLに差し替えて push
  2. 受付が終了 → カードごと削除するか、「受付終了」と明記して リンクを外す
  3. 一時的な障害（403/タイムアウト等）→ 手動で `workflow_dispatch` から再実行して確認
- 対応が済んだら Issue を閉じる。

## 3. 掲載の基準（追加・削除の判断）

- **一次情報のみ**: 企業・自治体・団体の公式発表ページへ直接リンクする。まとめ記事やSNSは根拠にしない。
- 追加時は「誰が / 何を / いつまで」をカード内に書けることを条件とする。
- 受付期間が終わったもの（例: 熊本県義援金は10/30予定）は期限翌週までに整理する。

## 4. 定期見直し（自動でリマインドされます）

毎週月曜6時（JST）に `ops-reminder.yml` が動き、その週に確認すべきことを Issue にして立てます。以下はその中身の定義です。

- **通知されるのは「確認すべきこと」だけで、サイトは自動更新されません。** 掲載の追加・修正は必ず公式発表を確認してから手で行うこと。日付を根拠に本文を自動生成すると、未確認の情報が公開される事故になります。
- 受付期限の**14日前**から通知が出ます。期限を過ぎたものは、整理するまで毎週出続けます。
- 期限やフェーズ別タスクを増やすときは `.github/ops-reminder.py` の `DEADLINES` / `PHASES` / `WEEKLY` を編集してください。ワークフロー本体は触らなくて構いません。



- **週1回**（毎週月曜目安）: 主要窓口の内容変更（受付期間の延長・終了、支援措置の追加）を各公式ページで確認する。
- 「支援を始める前に」セクションは、フェーズが進むと逆効果になりうる。災害ボランティアセンターが本格募集を始めたら、「募集状況を確認してから」という抑制的な書き方から、実際の募集窓口への案内に切り替えること（人手が必要な時期に人を止めてしまわないため）。
- **月1回**: サイト全体を通読し、フェーズに合わない情報（例: 発災直後の緊急情報）を復興フェーズの情報に入れ替える。

## 5. リポジトリ設定（一度だけ／自動化の前提）

上の自動化は、次の2つの設定が入っていないと動きません。ワークフローを追加した直後に必ず確認してください。

| 設定 | 場所 | 値 |
| --- | --- | --- |
| Pages の公開元 | Settings → Pages | Branch: `main` / Folder: `/ (root)` |
| Actions の既定権限 | Settings → Actions → General → Workflow permissions | **Read repository contents（read-only）** |

- Pages の公開元が `/docs` になっていると、`docs` ディレクトリが無いためビルドが失敗し、サイトは公開されません。
- Actions の既定権限は read-only で構いません。各ワークフローが必要な権限（`contents: write` / `issues: write`）を `permissions:` ブロックで明示しており、ワークフロー側の宣言は既定値より優先されるためです。既定を read-write にする必要はなく、最小権限の原則から read-only を推奨します。
- ルートの `.nojekyll` は、素の HTML を Jekyll に加工させずそのまま配信するためのファイルです。削除しないこと。

CLI で設定する場合:

```sh
gh api -X PUT /repos/yumaji/kumamoto-shien-repo/pages \
  -f "source[branch]=main" -f "source[path]=/"
gh api -X PUT /repos/yumaji/kumamoto-shien-repo/actions/permissions/workflow \
  -f default_workflow_permissions=read
```

## 6. セキュリティ

- **外部アクションはコミットSHAで固定する。** `uses: owner/action@v2` のようなタグ参照は、タグが動かされると別のコードが実行されうるため、`@<40桁のSHA> # vX.Y.Z` の形で固定しています。更新は Dependabot（`.github/dependabot.yml`、毎週月曜）が PR で提案するので、差分を見てマージすること。手でタグに戻さないこと。
- **CSP は `index.html` の meta タグで指定。** GitHub Pages はHTTPヘッダーを設定できないため meta で代替しています。画像やスクリプトなど外部リソースを追加したら、CSP の許可リストも同時に更新すること（更新しないと読み込みが無言でブロックされ、表示が壊れます）。
- **独自ドメインの検証（推奨・未実施）:** GitHub の Settings → Pages → Verified domains に `kumamoto-shien.net` を登録しておくと、万一 Pages を無効化した際に第三者がこのドメインを乗っ取って偽サイトを公開することを防げます。
- **OGP画像の作り直し:** SNSシェア時に出る `ogp.png` は `python3 tools/make-ogp.py` で再生成できます（要 Pillow）。サイトのタイトルやURLを変えたら、この画像も作り直すこと。手で画像編集ソフトを使う必要はありません。
- **アカウントの2要素認証:** GitHub とお名前.com の両方で有効にしておくこと。このサイトの実質的な攻撃経路は、サイト本体ではなくアカウントの乗っ取り（リポジトリ改竄・DNS書き換え）です。

## 7. やらないこと

- 未確認情報・伝聞の掲載
- 特定の寄付先への誘導や優先表示（並びはカテゴリ内で公的窓口→民間の順）
- 個人情報や被災者個人に関する情報の掲載
